function setupTheme(){const saved=localStorage.getItem('akb_theme');if(saved)document.documentElement.dataset.theme=saved;const nav=document.querySelector('.site-header nav');if(nav&&!nav.querySelector('.theme-toggle')){const button=document.createElement('button');button.className='theme-toggle';button.type='button';button.setAttribute('aria-label','Night mode');const paint=()=>button.textContent=document.documentElement.dataset.theme==='dark'?'☀':'☾';button.onclick=()=>{const next=document.documentElement.dataset.theme==='dark'?'light':'dark';document.documentElement.dataset.theme=next;localStorage.setItem('akb_theme',next);paint()};paint();nav.append(button)}const root=document.getElementById('songRoot');if(root?.dataset.song&&!document.querySelector('.breadcrumbs')){try{const song=JSON.parse(root.dataset.song);root.insertAdjacentHTML('beforebegin',`<nav class="breadcrumbs" aria-label="Breadcrumb"><a href="/">Home</a><span>›</span><a href="/?q=${encodeURIComponent(song.language)}">${song.language}</a><span>›</span><span>${song.title}</span></nav>`)}catch{}}}setupTheme();
